export type IName = {
    name: string;
}

export type ICategory = {
    id: number;
    name: string; 
    path?: string;
    description?: string;
    base_price?: number;
}

export type IId = {
    id: number;
}